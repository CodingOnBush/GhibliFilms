<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ghiblifilms">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.view.FilmsActivity"
            android:banner="@color/colorPrimary"
            android:theme="@style/AppTheme.Slide"
            android:label="Ghibli films" />
        <activity
            android:name=".presentation.view.DetailActivity"
            android:banner="@android:color/black"
            android:theme="@style/AppTheme.Slide"/>
        <activity
            android:name=".presentation.view.WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:banner="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
